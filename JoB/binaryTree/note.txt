ในขั้นตอนแรกใส่ Input เข้าไปเพื่อสร้าง node หลังจากนั้นก็จะสร้าง Tree
แล้วพอ print แแต่ละขั้นตอนในการสร้าง Tree ขึ้นมา
ขั้นตอนต่อไปหลังทำงานเสร็จแล้วก็จะเรียกใช้ function sort แล้ว print ค่าจาก Tree ออกมาเรียงจากน้อยไปมาก
แล้วทีนี้ก็ใส่ค่า node ที่จะลบ เข้าไปก็จะทำการลบ node ที่ใส่ไปแล้ว print ขั้นตอนการลบออกมา

